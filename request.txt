Enhance the existing StoneRate Admin Request page by adding support for viewing requests submitted by Transporters.

IMPORTANT SCOPE

This is a FRONTEND-ONLY UI task.

Do not create, edit, refactor, rename, or otherwise modify any backend file.

Do not modify:

- Buyer Request backend
- Transporter backend
- Database tables
- Database initialization
- Existing API endpoints
- Existing request-processing logic
- Existing Buyer Request data mapping
- Existing Buyer Request actions
- Existing Buyer Request cards
- Existing Buyer Request details navigation
- Existing Buyer Request filters
- Existing Buyer Request sorting
- Existing Buyer Request search
- Existing loading, error, or refresh behavior for Buyer Requests
- Existing Admin navigation

The existing Buyer Request section is already functional and must continue working exactly as it does now.

Only add:

1. A request-source filter at the top of the existing page
2. A new frontend card layout for viewing Transporter Requests
3. Temporary frontend data handling through props, an empty array, or a clearly separated placeholder data source

Do not invent or simulate production Transporter requests.

Do not connect the new Transporter view to any backend in this task.

The completed page must remain ready for backend integration later without requiring the Buyer Request implementation to be rewritten.

PRIMARY DESIGN REQUIREMENT

Focus strongly on making the updated page visually stunning, attractive, premium, modern, and production-ready.

The design should feel polished and professional while remaining consistent with the existing StoneRate Admin design language.

Use the existing Admin Request page as the main design reference.

Preserve its:

- Header
- Branding
- Typography
- Color system
- White canvas
- StoneRate orange accents
- Navigation
- Search design
- Card style
- Border radius
- Shadows
- Spacing
- Loading patterns
- Empty states
- Responsive behavior
- Mobile layout

The new source filter and Transporter Request cards must look like native parts of the existing page, not a separate application or unrelated design.

FILE TO UPDATE

Update the existing Admin Request page only.

Do not create a replacement page unless the existing project structure absolutely requires a small extracted frontend component.

If a small child component is created, keep it presentation-only and do not move or duplicate the existing Buyer Request logic.

PAGE PURPOSE

The existing Admin Request page should support two request sources:

1. By Buyer
2. By Transporter

The default view should remain:

By Buyer

The existing Buyer Request cards and behavior must remain unchanged under the By Buyer filter.

When Admin selects By Transporter, display Transporter Request cards.

REQUEST-SOURCE FILTER

Add a premium segmented control near the top of the page, below the page heading and above the existing request content.

The segmented control must contain:

- By Buyer
- By Transporter

Suggested internal values:

- buyer
- transporter

Default selection:

buyer

The filter should clearly indicate which request source is active.

Each option should include an appropriate icon:

- Buyer or user icon for By Buyer
- Truck or transport icon for By Transporter

The active option should have:

- StoneRate orange accent
- Stronger text
- Soft orange background or premium gradient
- Clear selected indicator
- Subtle shadow
- Smooth transition

The inactive option should remain:

- Clean
- Readable
- Accessible
- Easy to tap

Do not use a small ordinary dropdown for this primary source selection.

Use a premium segmented control, tab switcher, or pill-style selection control.

FILTER BEHAVIOR

When selectedSource is `buyer`:

- Render the current Buyer Request interface exactly as it currently works
- Use the current Buyer Request data
- Preserve existing search
- Preserve existing filters
- Preserve existing sorting
- Preserve existing action buttons
- Preserve existing View Request behavior
- Preserve current Buyer Request details navigation
- Preserve current API calls
- Preserve loading, error, and refresh states

When selectedSource is `transporter`:

- Render the new Transporter Request view
- Do not call the Buyer Request backend again merely because the filter changed
- Do not send a Transporter API request in this task
- Use the provided `transporterRequests` prop or an empty array
- Keep the implementation ready for future API integration
- Do not fabricate request records

Maintain Buyer and Transporter view states separately where useful.

For example:

- Buyer search should not unintentionally alter Transporter search
- Buyer filter values should remain intact when switching views
- Returning to By Buyer should show the previous Buyer Request state

OPTIONAL REQUEST COUNTS

If real counts are available through existing props, show them beside each source:

- By Buyer 12
- By Transporter 4

If no real Transporter count is available, do not invent one.

Do not display a fake zero if the data has not been loaded.

If the Transporter source is represented by an empty loaded array, showing zero is acceptable.

EXPECTED FRONTEND PROPS

The enhanced page should support the existing props without changing their behavior.

Add only the minimum new props needed for the Transporter view:

{
  transporterRequests = [],
  transporterLoading = false,
  transporterError = "",
  onRefreshTransporterRequests = () => {},
  onViewTransporterRequest = () => {}
}

These props are preparation for future integration only.

Do not add backend code for them.

If the existing page already has equivalent generic props, reuse the existing structure instead of adding duplicate props.

EXPECTED TRANSPORTER REQUEST DATA

Support Transporter Request objects using a render-safe structure such as:

{
  id: "TR-260925-001",
  status: "new",
  transporterId: "T100001",
  transporterName: "ABC Transport Agency",
  ownerName: "Registered Transporter",

  materialName: "40mm Crushed Stone",
  quantity: 500,
  quantityUnit: "feet",

  sellerId: "S100001",
  sellerName: "Stone Supplier",

  pickupLocation: "Dalla, Sonbhadra, Uttar Pradesh",

  requestedAt: "2026-09-25T10:30:00.000Z",
  responseExpiresAt: "2026-09-25T18:30:00.000Z",
  secondsLeft: 10800
}

The UI should safely support alternative backend field names where reasonable, but do not overcomplicate the page.

Do not hardcode the example record into production rendering.

TRANSPORTER REQUEST STATUSES

Support these statuses:

- new
- accepted
- rejected

Display them as:

- NEW
- ACCEPTED
- REJECTED

Use distinctive, accessible status badges:

NEW:
- Orange or blue accent
- Live indicator
- Represents an actionable incoming request

ACCEPTED:
- Green accent
- Check icon
- Represents an approved request

REJECTED:
- Red or muted-red accent
- Clear rejection indicator
- Represents a declined request

Do not determine a request status from appearance, time, or local assumptions.

Use only the status provided in the Transporter Request object.

TRANSPORTER REQUEST CARD

Each Transporter Request must appear as a polished, premium card.

The card must contain the following information:

1. Transporter Request ID

Label:

Transporter Request ID

Display the ID prominently using a readable monospaced style.

Example:

TR-260925-001

If the Request ID is missing, show:

Request ID unavailable

Do not invent an ID.

2. Request status

Place the status badge near the Request ID.

Supported status labels:

- NEW
- ACCEPTED
- REJECTED

3. Transporter name

Label:

Transporter

Display:

- Transporter agency name
- Transporter ID, when available
- Owner name, when available

The Transporter name should have strong visual hierarchy.

4. Material information

Display:

- Material name
- Requested quantity
- Quantity unit

Example:

40mm Crushed Stone
500 feet

Use a material or cube icon.

Handle unknown quantities safely.

Do not display:

0 quantity

unless zero is actually returned and is a valid backend value.

For missing quantity, show:

Quantity not provided

5. Seller information

Label:

Seller

Display:

- Seller name
- Seller ID, when available

If Seller information is missing, show:

Seller not assigned

Do not invent a Seller identity.

6. Pickup location

Label:

Pickup location

Display the full pickup address.

Use a location-pin icon.

Long addresses must wrap safely without producing horizontal overflow.

If the location is missing, show:

Pickup location not provided

7. Requested time

Label:

Requested at

Display the date and time using Indian date and time formatting.

Example:

25 Sep 2026, 10:30 AM

Use:

Asia/Kolkata

If `requestedAt` is missing, show:

Requested time not available

8. Response window timer

Show a live response-window timer when the request is new and a valid deadline is available.

Label:

Response window

Format:

HH : MM : SS

The timer should update once per second.

When less than 60 minutes remain:

- Show an urgent state
- Use a stronger warning accent
- Keep the timer readable
- Do not move or resize the card as the time changes

When less than 15 minutes remain:

- Use a critical visual treatment
- A subtle pulse is allowed
- Avoid excessive animation

When time reaches zero:

- Stop the countdown
- Show:
  Response window closed

Do not automatically change the backend request status from `new` to `rejected`.

The timer state is visual only.

When the request status is accepted:

- Replace the live timer with:
  Request accepted

When the request status is rejected:

- Replace the live timer with:
  Request rejected

If neither `responseExpiresAt` nor `secondsLeft` is available, show:

Response deadline not provided

9. View Request button

Add a strong primary button:

View Request

The button should:

- Be visually prominent
- Use the StoneRate orange theme
- Include an arrow or document icon
- Be easy to tap on mobile
- Remain aligned across cards
- Call:

onViewTransporterRequest(request)

Do not build the Transporter Request details page in this task.

Do not navigate to a fake page.

If `onViewTransporterRequest` is not connected, safely log or no-op without breaking the page.

CARD VISUAL STRUCTURE

Use a clear structure similar to:

Header:
- Request ID
- Status

Identity section:
- Transporter name
- Transporter ID
- Owner name

Request section:
- Material name
- Quantity
- Seller name
- Seller ID

Location section:
- Pickup location

Footer:
- Requested at
- Response window timer
- View Request button

Use polished visual details such as:

- Thin colored card rail based on status
- Soft gradients
- Subtle background mesh or pattern
- Premium shadows
- Clear iconography
- Compact informational chips
- Smooth hover elevation on desktop
- Strong visual hierarchy
- Consistent spacing
- Accessible contrast
- Subtle entrance animations

Do not make the cards overly decorative or difficult to scan.

The card should remain operational and professional.

TRANSPORTER VIEW TOOLBAR

When By Transporter is active, add a compact toolbar suitable for Transporter Requests.

Include:

1. Search field

Placeholder:

Search by Request ID, Transporter, Seller, material, or pickup location

Search should work on already-provided frontend data only.

Do not add a backend search request.

2. Status filter

Options:

- All
- New
- Accepted
- Rejected

3. Sort control

Options:

- Newest requested
- Oldest requested
- Ending soon
- Transporter name

Keep these Transporter-specific controls separated from the existing Buyer Request controls.

Do not change or reuse Buyer filter state in a way that affects the Buyer view.

TRANSPORTER SUMMARY COUNTS

Above the Transporter cards, display compact summary tiles or chips when real Transporter Request data is available:

- Total requests
- New
- Accepted
- Rejected

Calculate these values only from `transporterRequests`.

Do not mix Buyer Request counts with Transporter Request counts.

If Transporter data has not been connected, the empty state is more important than displaying unnecessary zero-heavy metrics.

LOADING STATE

When:

transporterLoading === true

Show attractive Transporter Request card skeletons.

The skeleton should approximate:

- Request ID
- Status badge
- Transporter information
- Material information
- Pickup location
- Timer
- View Request button

Do not display hardcoded example records while loading.

Do not replace the existing Buyer loading behavior.

ERROR STATE

When:

transporterError is not empty

Show:

Unable to load Transporter requests

Also display:

- The error message
- Retry button
- A way to switch back to By Buyer

The Retry button should call:

onRefreshTransporterRequests()

Do not affect Buyer Request error state.

EMPTY STATE

When By Transporter is active and there are no Transporter Requests, show an attractive empty state.

Title:

No Transporter requests available

Message:

Requests submitted by Transporters will appear here when Transporter Request integration is connected.

Include:

- Truck or inbox illustration/icon
- Refresh button, if `onRefreshTransporterRequests` is supplied
- Clear option to switch back to By Buyer

Do not generate sample cards.

Do not show fake Transporter orders or fake counts.

SEARCH EMPTY STATE

If Transporter Requests exist, but no records match the active search or filter, show:

No Transporter requests match the selected filters

Include:

- Clear search button
- Reset filters button

Do not use the full no-data empty state for a filtered result.

RESPONSIVE DESIGN

The enhancement must work well on mobile, tablet, and desktop.

MOBILE

On mobile:

- The By Buyer and By Transporter segmented control should span the available width
- Both options must remain easy to tap
- Cards should use one column
- Request ID and status should remain readable
- Transporter and Seller details should wrap safely
- Material and quantity should remain visually grouped
- Pickup address should wrap naturally
- Requested time and timer may stack
- View Request button should use full width
- Filter controls should stack or scroll safely
- No horizontal overflow is allowed

TABLET

On tablet:

- Use one or two card columns depending on available width
- Preserve readable spacing
- Keep timers and actions aligned

DESKTOP

On desktop:

- Use two or three card columns when space permits
- Keep all cards balanced
- Align action buttons consistently
- Do not stretch cards excessively
- Ensure long locations and names do not break the layout

ACCESSIBILITY

Include:

- Semantic buttons
- Accessible labels
- Descriptive status text
- Keyboard-operable source filters
- `aria-pressed` or appropriate tab semantics
- Visible keyboard focus styles
- Accessible timer labels
- Good color contrast
- Reduced-motion support
- Screen-reader-friendly loading and error states

Do not rely on color alone to communicate status.

FRONTEND IMPLEMENTATION RULES

1. Use React functional components and hooks.
2. Do not modify backend files.
3. Do not modify database files.
4. Do not add API endpoints.
5. Do not modify the Buyer Request backend.
6. Do not change existing Buyer Request API calls.
7. Do not change existing Buyer Request response handling.
8. Do not rewrite the Buyer Request card design unless necessary to place it under the By Buyer source filter.
9. Keep existing Buyer Request functionality unchanged.
10. Default to By Buyer.
11. Use a new, separate state for the request source.
12. Keep Buyer search and filters separate from Transporter search and filters.
13. Do not fabricate Transporter Request records.
14. Use `transporterRequests = []` when no data is provided.
15. Do not simulate successful API calls.
16. Do not create a Transporter Request details page.
17. Preserve current Admin navigation.
18. Preserve current CSS and component conventions.
19. Scope all new CSS to the existing page or the new Transporter Request section.
20. Avoid unnecessary third-party dependencies.
21. Use Indian number, currency, date, and time formatting where applicable.
22. Handle missing values safely.
23. Do not display internal errors directly unless supplied through `transporterError`.
24. Keep the page ready for backend integration later.
25. Return complete updated frontend code, not partial snippets.

RECOMMENDED STATE STRUCTURE

Use a source state such as:

const [requestSource, setRequestSource] = useState("buyer");

Use separate Transporter view state such as:

const [transporterQuery, setTransporterQuery] = useState("");
const [transporterStatus, setTransporterStatus] = useState("all");
const [transporterSort, setTransporterSort] = useState("newest");

Do not replace existing Buyer state with these variables.

RECOMMENDED RENDERING STRUCTURE

The page should conceptually render:

<ExistingAdminRequestHeader />

<RequestSourceSwitcher
  value={requestSource}
  onChange={setRequestSource}
/>

{requestSource === "buyer" ? (
  <ExistingBuyerRequestContent />
) : (
  <TransporterRequestContent
    requests={transporterRequests}
    loading={transporterLoading}
    error={transporterError}
    onRefresh={onRefreshTransporterRequests}
    onViewRequest={onViewTransporterRequest}
  />
)}

The actual implementation must follow the existing file’s structure and naming conventions.

Do not unnecessarily extract the current Buyer Request section into another file if doing so introduces risk.

If safer, wrap the current Buyer rendering with:

requestSource === "buyer"

and add the new Transporter rendering alongside it.

FINAL OUTPUT REQUIREMENT

Return:

1. The complete updated existing Admin Request page file
2. Any new presentation-only frontend component, only if genuinely required
3. A concise summary of the frontend additions
4. A clear statement that no backend changes were made
5. A clear statement that existing Buyer Request functionality was preserved
6. A list of the optional future props required for Transporter backend integration

Do not provide:

- Backend files
- Backend endpoints
- Database migrations
- Mock API services
- Fake Transporter Request records
- Changes to Buyer Request backend
- A Transporter Request details page

The final result must look stunning, attractive, modern, premium, smooth, responsive, and fully consistent with the existing StoneRate Admin Request page.
