Create a complete, production-quality React page for the StoneRate Admin interface named:

StoneRateAdminTransporterRequestDetails.js

PURPOSE

This page allows the StoneRate Admin to inspect and process a material request submitted by a registered Transporter.

The Admin must be able to:

- Review the Transporter Request ID and status
- Review when the request was submitted
- Monitor the response-window countdown
- Review Transporter identity and contact information
- Review the requested material and quantity
- View the permanent material reference image
- Review the material rate and permit cost
- Review Seller details
- Call the Transporter
- Call the Seller
- Open navigation to the Seller’s pickup location
- Accept the Transporter request
- Reject the Transporter request with a structured reason
- Open a detailed material reference popup
- Open a detailed Transporter information popup

IMPORTANT DESIGN INSTRUCTION

Use the existing:

AdminRateRequestDetails.js

page as the primary design and theme reference.

The new page must inherit its:

- StoneRate Admin visual identity
- Header structure
- White canvas
- Orange accents
- Typography
- Spacing system
- Card treatment
- Information hierarchy
- Form controls
- Buttons
- Modal design
- Shadows
- Border radius
- Loading skeletons
- Error states
- Mobile responsiveness
- Interaction patterns

Do not create an unrelated visual system.

Focus strongly on making the page visually stunning, attractive, modern, premium, polished, and production-ready.

The page should look impressive while remaining professional, readable, and easy for the Admin to operate.

Use:

- Subtle gradients
- Premium layered cards
- Soft shadows
- Purposeful iconography
- Status-based colored accents
- Carefully spaced information blocks
- Smooth but restrained animations
- Clear action hierarchy
- Elegant countdown presentation
- Attractive confirmation dialogs

Avoid:

- Excessive decoration
- Cluttered cards
- Oversized headings
- Poorly aligned information
- Unnecessary animation
- Horizontal overflow
- Unrelated colors

FILE TO CREATE

Create the complete working code for:

src/pages/StoneRateAdminTransporterRequestDetails.js

SCOPE OF THIS STEP

Create the complete frontend page and interaction logic.

Do not modify any backend file in this step.

Do not modify:

- Buyer Request backend
- Existing Buyer Request APIs
- Buyer Request details page
- Existing Admin Rate Request workflow
- Database initialization
- Transport bidding backend
- Existing Seller APIs
- Existing Transporter registration APIs

The new page should be callback-driven and ready for backend connection later.

Do not fabricate production request data.

PAGE COMPONENT PROPS

The page should accept:

{
  selectedRequest = null,
  loading = false,
  error = "",
  saving = false,

  onBack = () => {},
  onRefresh = () => {},
  onRetry = () => {},

  onAcceptRequest = async () => {},
  onRejectRequest = async () => {},

  onCallTransporter = () => {},
  onCallSeller = () => {},
  onNavigateSeller = () => {},

  onHome = () => {},
  onSamples = () => {},
  onTransporterRequests = () => {},
  onRateRequests = () => {},
  onConfirmedOrders = () => {}
}

EXPECTED REQUEST DATA

Support request data such as:

{
  id: "TR-260925-001",
  status: "new",

  requestedAt: "2026-09-25T10:30:00.000Z",
  responseExpiresAt: "2026-09-25T18:30:00.000Z",
  secondsLeft: 10800,

  transporter: {
    id: "T100001",
    name: "ABC Transport Agency",
    ownerName: "Registered Transporter",
    phone: "9876543210",
    location: "Varanasi, Uttar Pradesh",
    address: "Registered transport office address",
    city: "Varanasi",
    state: "Uttar Pradesh",
    pincode: "221001",
    joinedAt: "2026-08-15T10:30:00.000Z",
    phoneVerified: true,
    adminVerified: true
  },

  material: {
    id: "material-1",
    name: "40mm Crushed Stone",
    materialType: "Stone",
    quantity: 500,
    quantityUnit: "feet",

    sampleId: "sample-1790186678980-b16yakch",
    sampleCode: "2CS-MUEXDJMG-08BS",

    imageUrl: "https://example.com/material-image.jpg",
    thumbnailUrl: "https://example.com/material-thumbnail.jpg",

    rate: 1277,
    rateUnit: "ton",

    feetPerTon: 45,
    convertedRate: 28.38,
    convertedRateUnit: "feet",

    permitCost: 850,
    availableQuantity: 80,
    availableQuantityUnit: "ton",

    sampleUploadedAt: "2026-09-25T06:00:00.000Z",
    sampleExpiresAt: "2026-09-26T23:43:38.000Z"
  },

  seller: {
    id: "S100000",
    name: "Registered Seller",
    plantName: "Stone Supplier",
    phone: "7348145995",

    location: "Sonbhadra, Sonbhadra, Uttar Pradesh, 166484",
    address: "Registered Seller address",
    city: "Sonbhadra",
    state: "Uttar Pradesh",
    pincode: "166484",

    latitude: 24.6869,
    longitude: 83.0684,

    joinedAt: "2026-07-20T09:30:00.000Z"
  },

  acceptedAt: null,
  rejectedAt: null,
  rejectionReason: "",
  rejectionNote: ""
}

Do not hardcode this example into the rendered page.

Use the values only to define the supported frontend structure.

PAGE STATUS SUPPORT

Support these request statuses:

- new
- accepted
- rejected

Display them as:

- NEW
- ACCEPTED
- REJECTED

STATUS VISUALS

NEW:

- Orange or blue live accent
- Active response timer
- Accept and Reject buttons enabled
- Subtle live indicator

ACCEPTED:

- Green status badge
- Check icon
- Show accepted date and time, when available
- Disable or hide Accept and Reject actions
- Show:
  Request accepted

REJECTED:

- Red or muted-red status badge
- Rejection icon
- Show rejected date and time
- Show rejection reason and note
- Disable or hide Accept and Reject actions
- Show:
  Request rejected

Use the backend-provided status.

Do not infer accepted or rejected status from local time.

PAGE HEADER

Create a header consistent with AdminRateRequestDetails.js.

The header must contain:

1. Back button
2. StoneRate Admin branding
3. Page title:
   Transporter Request Details
4. Request ID
5. Request status badge
6. Refresh button
7. Supporting text:
   Review the material request, contact the parties, and record the Admin decision

When request information has not loaded, do not show fake identifiers.

Show a loading placeholder instead.

REQUEST SUMMARY SECTION

Create a premium summary card near the top of the page.

Display:

- Transporter Request ID
- Current request status
- Requested date and time
- Response expiry date and time
- Live response timer
- Material name
- Requested quantity
- Seller name
- Pickup location

The Request ID should use a monospaced style and remain readable on mobile.

REQUESTED AT

Show:

Requested at

Format the timestamp using:

- Indian date formatting
- Indian time formatting
- Asia/Kolkata timezone

Example:

25 Sep 2026, 10:30 AM

If unavailable, show:

Requested time not available

RESPONSE WINDOW TIMER

For a new request, display a live timer:

HH : MM : SS

The timer should update once every second.

The timer should be visually attractive and stable.

Use separate visual units for:

- Hours
- Minutes
- Seconds

When more than 60 minutes remain:

- Use a standard active state
- Use StoneRate orange or complementary blue accent

When 60 minutes or less remain:

- Show:
  Ending soon
- Use an urgent amber or orange treatment

When 15 minutes or less remain:

- Use a critical red treatment
- A restrained pulsing indicator is allowed
- Do not cause layout movement

When the timer reaches zero:

- Stop the countdown
- Show:
  Response window closed
- Disable Accept
- Disable Reject
- Preserve all request information

Do not automatically change the request status to rejected in frontend state.

If the request status is accepted:

- Stop the live countdown
- Show:
  Request accepted

If the request status is rejected:

- Stop the live countdown
- Show:
  Request rejected

If no valid deadline exists, show:

Response deadline not provided

TRANSPORTER INFORMATION SECTION

Create a dedicated Transporter card.

Display:

- Transporter agency name
- Transporter ID
- Owner name, when available
- Registered location
- City and state
- Mobile number
- Joining date
- Request status context

Include:

1. Call button
2. Clickable Transporter name
3. Clickable Transporter ID

The call button should call:

onCallTransporter(
  selectedRequest.transporter.phone,
  selectedRequest.transporter
)

If the phone number is unavailable:

- Disable the Call button
- Show an accessible explanation
- Do not generate a phone number

The call button should be visually prominent but should not compete with the final Accept action.

TRANSPORTER DETAILS POPUP

When Admin clicks the Transporter name or Transporter ID, open a polished Transporter Details popup.

Popup title:

Transporter Details

Display:

- Transporter agency name
- Transporter ID
- Owner name
- Mobile number
- Registered location
- Address
- City
- State
- Pincode
- Joining date
- Phone verification status, when available
- Admin verification status, when available
- Current request ID
- Requested material
- Requested quantity
- Requested date and time
- Call Transporter button
- Copy Transporter ID button
- Close button

The popup Call button should call:

onCallTransporter(phone, transporter)

The Copy button should copy the Transporter ID and show:

Transporter ID copied

Do not expose:

- Aadhaar details
- GST documents
- OTP information
- Internal authentication information
- Personal verification document images

MATERIAL INFORMATION SECTION

Create a dedicated material card showing:

- Material reference image
- Material name
- Material type
- Requested quantity
- Quantity unit
- Published material rate
- Rate unit
- Converted material rate, when applicable
- Feet-per-ton conversion, when applicable
- Permit cost
- Available quantity
- Sample code
- Source sample ID
- Sample upload date
- Sample expiry date

The reference image must be clickable.

Display the material reference image attractively, with:

- Rounded corners
- Proper image framing
- Loading state
- Broken-image state
- Reference badge
- Hover or tap affordance
- Accessible alternative text

Do not force-crop the image in the full reference popup.

MATERIAL RATE DISPLAY

Display:

Material rate

Example:

₹1,277 / ton

When applicable, display:

Converter rate

Example:

1 ton = 45 feet

Display:

Converted material rate

Example:

₹28.38 / feet

Display:

Permit cost

Example:

₹850

If values are unavailable, show:

Not provided

Do not convert or invent commercial values in the UI unless the backend provides the conversion inputs.

REFERENCE SOURCE POPUP

When Admin clicks the material reference image, open a detailed popup based on the supplied Reference Source screenshot.

The popup must visually follow the reference structure:

- White modal surface
- Clear title area
- Close button in the upper-right corner
- Large material image
- Structured label-and-value rows
- Copy action
- Call Seller action
- Privacy note at the bottom

Popup title:

Reference Source

Show:

1. Sample or reference code
2. Large material image
3. Uploaded by
4. Seller name
5. Seller ID
6. Material type
7. Source sample ID
8. Location
9. Mobile number
10. Uploaded date and time
11. Recorded material rate
12. Converter rate
13. Converted material rate
14. Sample expiry date and time
15. Permit cost
16. Available quantity
17. Copy information button
18. Call Seller button
19. Close button

The uploaded visual reference uses a large image followed by compact information rows for Seller ID, material type, source sample ID, location, mobile, uploaded time, recorded rate, converter rate, converted material rate, sample expiry, permit cost, and available quantity. Reproduce this information hierarchy while adapting the visual styling to the existing StoneRate Admin theme.

LARGE IMAGE BEHAVIOR

The image in the Reference Source popup should:

- Preserve the full source image
- Use object-fit: contain
- Avoid forced cropping
- Display a loading skeleton
- Display a broken-image fallback
- Include accessible alternative text
- Support opening a larger lightbox when clicked

The lightbox should:

- Use a dark image-viewing background
- Preserve the source aspect ratio
- Provide a clear Close button
- Close with Escape
- Return focus correctly

COPY INFORMATION ACTION

The Copy Information button should copy a clean summary containing:

- Request ID
- Material name
- Sample code
- Seller name
- Seller ID
- Location
- Mobile number
- Material rate
- Converter rate
- Converted material rate
- Permit cost
- Available quantity
- Sample expiry

Show:

Reference information copied

if successful.

Show:

Copy failed

if clipboard access fails.

CALL SELLER ACTION

The Call Seller button in the reference popup should call:

onCallSeller(
  selectedRequest.seller.phone,
  selectedRequest.seller
)

If the Seller phone is missing:

- Disable the button
- Do not create a placeholder number

Include this privacy note:

Seller information is internal and will not be shown to the Buyer or Transporter unless StoneRate’s workflow explicitly allows it.

SELLER DETAILS SECTION

Create a dedicated Seller card below or alongside the material section.

Display:

- Seller name
- Seller ID
- Plant or business name, when available
- Mobile number
- Registered location
- Address
- City
- State
- Pincode
- Joining date, when available
- Material supplied
- Sample code

Include two action buttons:

1. Navigate
2. Call Seller

NAVIGATE BUTTON

The button label should be:

Navigate

Use a route or navigation icon.

On clicking, call:

onNavigateSeller({
  seller: selectedRequest.seller,
  material: selectedRequest.material,
  request: selectedRequest
})

The navigation payload should include:

- Seller name
- Seller ID
- Full pickup address
- City
- State
- Pincode
- Latitude and longitude, when available
- Material name
- Request ID

Do not create coordinates if they are missing.

If coordinates are unavailable but the address exists, allow the parent handler to navigate using the address.

Disable the Navigate button only if there is neither:

- A usable address
- Valid coordinates

Do not directly hardcode a third-party map URL if the project already uses a shared navigation handler.

CALL SELLER BUTTON

The button label should be:

Call Seller

On clicking, call:

onCallSeller(
  selectedRequest.seller.phone,
  selectedRequest.seller
)

Disable the action if the Seller phone number is unavailable.

SELLER IDENTITY INTERACTION

Make the Seller name and Seller ID clickable.

Clicking either can open a Seller Details popup.

SELLER DETAILS POPUP

Display:

- Seller name
- Seller ID
- Plant name
- Mobile number
- Registered location
- Full address
- City
- State
- Pincode
- Joining date
- Material name
- Published material rate
- Permit cost
- Available quantity
- Sample code
- Sample upload date
- Sample expiry date
- Navigate button
- Call Seller button
- Copy Seller ID button
- Close button

Do not expose unnecessary Seller verification documents.

FINAL DECISION SECTION

At the bottom of the page, create a sticky or strongly visible decision panel.

The panel title should be:

Request Decision

Supporting text:

Review the Transporter, material reference, and Seller availability before recording the final decision.

For a new request with an active response window, show:

1. Reject Request button
2. Accept Request button

The Accept Request button should be the primary action.

The Reject Request button should be a strong secondary destructive action.

On mobile:

- Both buttons should be large and easy to tap
- Stack the buttons if required
- Keep actions visible without covering important content
- Respect safe-area spacing

Do not show active decision buttons if the request is already accepted or rejected.

ACCEPT REQUEST WORKFLOW

When Admin clicks:

Accept Request

open a confirmation popup.

Popup title:

Accept this Transporter request?

Show a concise summary:

- Request ID
- Transporter name
- Transporter ID
- Material name
- Requested quantity
- Seller name
- Pickup location
- Material rate
- Permit cost
- Remaining response time

Confirmation message:

Accepting this request confirms that StoneRate can proceed with the selected material and Seller for the Transporter.

Actions:

- Cancel
- Accept Request

On confirmation, call:

onAcceptRequest({
  requestId: selectedRequest.id,
  transporterId: selectedRequest.transporter.id,
  sellerId: selectedRequest.seller.id,
  materialId: selectedRequest.material.id,
  sampleId: selectedRequest.material.sampleId
})

During submission:

- Disable both actions
- Show a saving indicator
- Prevent duplicate submissions
- Do not close the popup until the backend confirms success

After successful acceptance:

- Close the popup
- Show:
  Transporter request accepted successfully
- Refresh the request details
- Display status as ACCEPTED
- Disable further decisions

If the request fails:

- Keep the popup open
- Display the backend error
- Re-enable actions

Do not update the final request status only in local state before backend success.

REJECT REQUEST WORKFLOW

When Admin clicks:

Reject Request

open a structured rejection popup.

Popup title:

Reject Transporter Request

Supporting text:

Select the reason that best explains why this request cannot be accepted.

REJECTION REASONS

Show these options:

1. Seller not picking up the call
2. Material is no longer available
3. Other

Use attractive radio cards or selectable reason tiles.

Do not use an ordinary unstyled radio list.

OPTION 1

Display:

Seller not picking up the call

Internal value:

seller_not_answering

OPTION 2

Display:

Material is no longer available

Internal value:

material_unavailable

OPTION 3

Display:

Other

Internal value:

other

OTHER REASON NOTES

When Other is selected, reveal a notes field.

Label:

Rejection notes

Placeholder:

Explain why this Transporter request is being rejected

The notes field should:

- Be required for Other
- Support multiple lines
- Have a character counter
- Have a sensible maximum length, such as 500 characters
- Display validation feedback
- Preserve the entered value if the submission fails

For the predefined reasons, optionally allow an Admin note if the existing application convention supports it.

Reject payload:

onRejectRequest({
  requestId: selectedRequest.id,
  reasonCode,
  reasonLabel,
  note
})

REJECT CONFIRMATION

Before final submission, either:

- Use the same rejection modal with a clear final confirmation action, or
- Show a second compact confirmation step

The final warning should state:

Rejecting this request will stop the current material-request workflow for the Transporter.

Actions:

- Keep Request Open
- Reject Request

During rejection:

- Disable all modal controls
- Show progress
- Prevent duplicate submission
- Keep the popup open if the API fails

After successful rejection:

- Close the popup
- Show:
  Transporter request rejected
- Refresh the details
- Display status as REJECTED
- Show the selected rejection reason
- Show the Admin note, when available
- Disable Accept and Reject actions

Do not delete the request.

A rejected request must remain visible in request history.

ACTION AVAILABILITY RULES

NEW AND TIMER ACTIVE:

- Call Transporter enabled when phone exists
- Call Seller enabled when phone exists
- Navigate enabled when location exists
- Accept enabled
- Reject enabled
- Reference image popup enabled

NEW BUT TIMER EXPIRED:

- Accept disabled
- Reject disabled
- Call actions may remain available
- Navigate may remain available
- Reference information remains visible
- Show:
  Response window closed

ACCEPTED:

- Accept hidden or disabled
- Reject hidden or disabled
- Call actions remain available
- Navigate remains available
- Reference information remains visible
- Show acceptance timestamp

REJECTED:

- Accept hidden or disabled
- Reject hidden or disabled
- Call actions may remain available
- Navigate may remain available
- Reference information remains visible
- Show rejection reason
- Show rejection note
- Show rejection timestamp

LOADING STATE

When loading is true and no request is loaded, show carefully designed skeletons for:

- Header Request ID
- Status
- Request summary
- Requested time
- Timer
- Transporter card
- Material image and details
- Seller card
- Decision buttons

Do not show hardcoded sample information while loading.

REFRESHING STATE

If a request is already visible and a refresh begins:

- Keep the existing information visible
- Apply a subtle refreshing state
- Disable duplicate actions
- Animate the refresh icon
- Avoid replacing the entire page with skeletons

ERROR STATE

If request details fail to load, show:

Unable to load Transporter request details

Display:

- API error message
- Retry button
- Back button

If refresh fails while older data exists:

- Keep older data visible
- Show a non-blocking warning
- Include a Retry action

EMPTY STATE

If no request is selected, show:

No Transporter request selected

Message:

Open a Transporter request from the Admin Request page to view its details.

Include:

- Back to Requests button
- Transport request icon

SUCCESS AND ERROR FEEDBACK

Use polished toast or inline feedback for:

- Request accepted successfully
- Request rejected successfully
- Transporter ID copied
- Seller ID copied
- Reference information copied
- Call information unavailable
- Navigation information unavailable
- API operation failed
- Refresh failed

API errors must remain understandable and should not silently close the action popup.

RESPONSIVE DESIGN

The page must be fully responsive.

MOBILE

On mobile:

- The header must remain compact
- Request ID and status must wrap safely
- Requested time and timer may stack
- Timer units must remain readable
- Transporter, material, and Seller cards must use full width
- Material image should remain prominent
- Seller location must wrap naturally
- Navigate and Call buttons should be easy to tap
- Accept and Reject actions should be easy to reach
- Modals should use nearly the full viewport
- Modal content should scroll independently
- Modal headers should remain visible
- Modal action buttons should respect safe-area spacing
- No horizontal overflow is allowed

TABLET

On tablet:

- Use a balanced one- or two-column layout
- Keep material image and details aligned
- Maintain readable card spacing

DESKTOP

On desktop:

- Use a polished two-column layout where appropriate
- Request summary may span the full width
- Transporter and Seller cards may appear in balanced columns
- Material reference information should remain visually prominent
- Decision actions should have a clear full-width section
- Avoid excessive empty space
- Do not stretch text blocks excessively

MODAL ACCESSIBILITY

Every popup must include:

- role="dialog"
- aria-modal="true"
- Accessible title
- Accessible close button
- Escape-key closing
- Focus trapping
- Focus restoration after closing
- Click-outside closing when not submitting
- Scroll locking
- Visible focus styles
- Disabled close behavior during critical submission, if appropriate

GENERAL ACCESSIBILITY

Include:

- Semantic headings
- Semantic buttons
- Accessible labels
- Accessible status announcements
- Accessible countdown label
- Good contrast
- Keyboard navigation
- Alternative text for images
- Reduced-motion support
- Status text in addition to color
- Descriptive button labels

SECURITY AND PRIVACY

- Do not expose OTP values
- Do not expose Aadhaar information
- Do not expose internal authentication records
- Do not expose verification document images
- Do not expose database IDs when a public ID is available
- Do not trust frontend decision status
- Do not trust frontend commercial calculations
- Backend must validate whether the response window is open
- Backend must ensure the request is still new before accepting or rejecting it
- Backend must record the Admin decision time
- Backend must preserve the rejection reason
- Backend must prevent duplicate final decisions

FRONTEND IMPLEMENTATION RULES

1. Use React functional components and hooks.
2. Follow the existing project’s file and import conventions.
3. Do not install unnecessary dependencies.
4. Do not use an external CSS framework.
5. Keep all new styles scoped to this page.
6. Use the project’s existing icons where possible.
7. Do not fabricate request information.
8. Do not mutate the selectedRequest prop.
9. Use Indian date and currency formatting.
10. Use Asia/Kolkata for displayed timestamps.
11. Handle every missing property safely.
12. Do not display ₹0 when a commercial value is unavailable.
13. Do not automatically reject an expired request.
14. Do not complete actions only in frontend state.
15. Wait for callback success.
16. Refresh request details after successful actions.
17. Prevent duplicate submissions.
18. Preserve error messages inside the active popup.
19. Keep timer updates stable.
20. Stop unnecessary timers when the page unmounts.
21. Do not create backend code in this step.
22. Do not modify the Buyer Request backend.
23. Do not modify the current Buyer Request page.
24. Do not modify the Admin Transport Bidding workflow.
25. Return complete, production-ready page code.

RECOMMENDED INTERNAL COMPONENTS

The page may use internal components such as:

- RequestHeader
- RequestSummaryCard
- ResponseTimer
- TransporterCard
- TransporterDetailsModal
- MaterialRequestCard
- ReferenceSourceModal
- ImageLightbox
- SellerCard
- SellerDetailsModal
- AcceptRequestModal
- RejectRequestModal
- StatusBanner
- LoadingSkeleton
- ErrorState
- Toast

Keep these components inside the same file unless the existing project convention clearly prefers separate files.

PAGE INTEGRATION EXPECTATION

The existing Admin Request page should call:

onViewTransporterRequest(request)

The expected App.js flow is:

1. Admin opens the existing Admin Request page.
2. Admin selects:
   By Transporter
3. Admin clicks:
   View Request
4. App.js stores the selected Transporter Request or its ID.
5. App.js opens:
   adminTransporterRequestDetails
6. The details page loads the latest request information.
7. Back returns to the Admin Request page with:
   By Transporter
   still selected.

Do not connect this flow in the page file itself.

Keep navigation callback-driven.

EXPECTED FUTURE API FUNCTIONS

The frontend page should be ready to work with:

getAdminTransporterRequestDetails(requestId)

acceptAdminTransporterRequest(requestId, payload)

rejectAdminTransporterRequest(requestId, payload)

Do not implement these API functions in this task.

FINAL OUTPUT REQUIREMENTS

Return:

1. Complete code for:

src/pages/StoneRateAdminTransporterRequestDetails.js

2. The exact expected request-data structure

3. Required callback signatures

4. Exact App.js integration instructions

5. A list of future Admin API functions required

6. A clear statement that no backend code was changed

7. A clear statement that the existing Buyer Request backend and Buyer Request workflow were not touched

The finished page must be stunning, attractive, modern, premium, responsive, accessible, and fully consistent with AdminRateRequestDetails.js.
